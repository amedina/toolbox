package com.medina.toolbox.puzzles;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ClockAngles {

	private static Logger log = LoggerFactory.getLogger(ClockAngles.class);
	
	public float getAngle(int hour, int min) {
	
		/* Compute hour arm's angle */
		
		
		/* Compute minute arm's angle */
		
		/* Subtract them to obtain final result */
		
		
		return 0f;
	}
	
	public static void main(String[] args) {
		
		int[][] A = new int[10][10];
		
		log.info("A.l: {}", A.length);
		log.info("A.l: {}", A[0].length);
		log.info("{}:{}", true, !true);
	}

}
